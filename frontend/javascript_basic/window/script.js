let val;

val = window.outerHeight;
val = window.outerWidth;

val = window.innerHeight;
val = window.innterWidth;

val = window.scrollY; //스크롤위치

val = window.location;

//window.location.href = "http://google.com";

val = window.history;
val = window.navigator;
console.log(val);
